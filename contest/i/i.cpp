#include <bits/stdc++.h>
#include <contest/debug.hpp>

#include <lib/prelude.hpp>

void solve() {
}

i32 main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);

    i32 t;
    std::cin >> t;

    while (t--) {
        solve();
    }
}