#include <bits/stdc++.h>

using i64 = long long;

auto solve() {
}

auto main() -> int {
        std::ios_base::sync_with_stdio(false);
        std::cin.tie(nullptr);

        int t;
        std::cin >> t;

        while (t--) {
                solve();
        }
}
