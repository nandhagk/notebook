#include <bits/stdc++.h>
#include <lib/prelude.hpp>

auto solve() {
}

auto main() -> i32 {
        std::ios_base::sync_with_stdio(false);
        std::cin.tie(nullptr);

        i32 t;
        std::cin >> t;

        while (t--) {
                solve();
        }
}
